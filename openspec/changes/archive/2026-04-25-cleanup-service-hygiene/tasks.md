## 1. Backend Tests

- [x] 1.1 Codexのみ選択した生成で `.codex/rules/general.md` と共通テンプレートだけが生成結果に含まれることをテストする
- [x] 1.2 同一プロジェクトで再生成したとき、同じ `file_path` の生成ファイルが重複作成されず更新されることをテストする
- [x] 1.3 ZIPエクスポートで保存済み `file_path` と `content` がそのまま出力され、編集後内容も反映されることをテストする

## 2. Backend Service Cleanup

- [x] 2.1 回答値をリスト化する処理を意図が分かるヘルパーとして整理し、既存の空値・単一値・配列値の扱いを維持する
- [x] 2.2 `generator.py` のテンプレート選択処理を分離し、AIツール別テンプレートの条件判定を読みやすくする
- [x] 2.3 `generator.py` のテンプレート描画処理とコンテキスト作成処理を分離し、生成内容の互換性を維持する
- [x] 2.4 `generator.py` の既存生成ファイル更新処理を分離し、再生成時の重複防止を明確にする
- [x] 2.5 `export.py` のZIP書き込み処理を小さなヘルパーに整理し、保存済みパスと内容を変更せずに出力する

## 3. Frontend Verification

- [x] 3.1 フロントエンド変更が不要であることを確認し、既存APIレスポンス形式に依存する画面フローが変わらないことを確認する

## 4. Verification

- [x] 4.1 バックエンドのテストを実行し、生成・再生成・編集・エクスポートの既存フローが通ることを確認する
- [x] 4.2 利用可能なlintまたは型チェックを実行し、サービス層整理による品質低下がないことを確認する
