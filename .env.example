# Mac 上で Postgres が localhost:5432（コンテナ外）のときは backend のみ起動し、
# ホスト名に host.docker.internal を使う（コンテナ内の localhost はコンテナ自身を指す）。
# docker compose up backend --no-deps
DATABASE_URL=postgresql+psycopg2://harness:harness@host.docker.internal:5432/harness_builder
